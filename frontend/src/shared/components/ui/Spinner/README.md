# 🌀 Spinner Component

The `<Spinner>` component provides accessible loading indicators for buttons, forms, page transitions, and data fetching.

## Usage Example

```jsx
import { Spinner } from '@/shared/components/ui';

<Spinner size="md" variant="primary" showLabel label="Loading products..." />
```
